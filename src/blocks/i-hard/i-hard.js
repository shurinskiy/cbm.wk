import Swiper from 'swiper';

(() => {

	new Swiper(".i-hard__items", {
		spaceBetween: 30,
		slidesPerView: 2.7,
		slidesOffsetBefore: 15,
		loop: true,
		// observeParents: true,
		// observer: true,

	});

})();
